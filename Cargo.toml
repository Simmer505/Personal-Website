[package]
name = "personal-website"
version = "0.1.0"
edition = "2021"


[dependencies]
tokio = { version = "1.36.*", features = ["full"] }
axum = "0.7.*"
tower-http = { version = "0.5.*", features = ["fs"] }
